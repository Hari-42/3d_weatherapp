export const PLATFORM_RADIUS = 8.4;

/**
 * Uniform scale applied to the whole diorama (city + its weather) so the OSM
 * city and the weather effects stay proportional to the small floating island.
 */
export const CITY_SCALE = 0.27;
